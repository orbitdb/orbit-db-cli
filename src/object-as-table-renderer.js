'use strict'

const table = require('./table')
const schemaView = require('./csv-schema1-view.js')

const objectAsTableRenderer = (objects) => {
  const headerRows = 
  return headerRows
}

module.exports = objectAsTableRenderer
